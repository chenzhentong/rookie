<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="per.wkz.rookie.dao.UserDao">

    <insert id="addUser" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into user(name,account,password,phone,type,email)
        values(#{name},#{account},#{password},#{phone},#{type},#{email})
    </insert>

    <update id="updateUser" parameterType="User">
        update user set id = #{id}
        <if test="email!=null">
            ,email=#{email}
        </if>
        where id = #{id}
    </update>

    <select id="getUserByCriteria" parameterType="UserSearchCriteria" resultMap="UserResultMap">
        select *
        from user where 1 = 1
        <if test="account!=null">
            and account=#{account}
        </if>
        <if test="password!=null">
            and password=#{password}
        </if>
    </select>
    <select id="getUserById" resultMap="UserResultMap">
        select *
        from user
        where id = #{id}
        limit 1
    </select>

    <select id="getUserByAccountAndPwd" resultMap="UserResultMap">
        select *
        from user
        where account = #{account}
          and password = #{password}
          and type = #{type}
        limit 1
    </select>

    <select id="getUserByPhone" resultMap="UserResultMap" parameterType="String">
        select *
        from user
        where phone = #{phone}
    </select>
    <update id="updatePassword">
        update user
        set password = #{password}
        where id = #{id}
    </update>

    <resultMap id="UserResultMap" type="User">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="account" property="account" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="type" property="type" jdbcType="INTEGER"/>
        <result column="IDCard" property="IDCard" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
    </resultMap>
</mapper>